document.addEventListener('DOMContentLoaded', function () {
    // Select the element with ID 'all-gravy'
    const allGravyElement = document.getElementById('all-gravy');

    // Set the display style to 'block'
    if (allGravyElement) {
        allGravyElement.style.display = 'block';
    }
});
